import React from 'react';
import { UploadOutlined } from '@ant-design/icons';

const UploadIcon = ({ size = 48, color = '#1677ff' }) => (
  <UploadOutlined style={{ fontSize: size, color }} />
);

export default UploadIcon;
